node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
__pycache__/
*.pyc

# Planilhas de origem nunca entram no repo: contem dado de saude identificado
*.xlsx
*.xls

# ---------------------------------------------------------------------------
# DADO PESSOAL SENSÍVEL (LGPD art. 5º II e art. 11)
# Nome, matrícula e diagnóstico de colaboradores identificados.
# Estes arquivos SÓ podem entrar no git se o repositório for PRIVADO.
# ---------------------------------------------------------------------------
# A pasta inteira, nao so os .json: qualquer copia de dado real que caia
# aqui (um .sql baixado, um .csv exportado) fica de fora por padrao.
dados/

# E o seed, em qualquer lugar do repo.
*_dados.sql
