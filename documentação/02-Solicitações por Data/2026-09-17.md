---
tags: [solicitacao, "2026-09-17"]
---

# 2026-09-17 — MFA parou de funcionar, segunda rodada de profissionalização

Quinto dia. Começou com um relato: "ativei o MFA, funcionou, depois parou".
Terminou com o incidente resolvido e mais uma fatia dos gaps levantados em
16/09 atacada.

## Incidente de MFA — causa raiz e correção

Diagnóstico por eliminação, com o usuário rodando consultas no SQL Editor:

- `pg_proc` confirmou as 5 funções (`mfa_ok`, `tenho_mfa`, `mfa_obrigatorio`,
  `adesao_mfa`, `meu_papel`) — migrations `0011`–`0013` estavam aplicadas.
- `configuracoes.mfa_obrigatorio = false` — ninguém estava travado por RLS.
- `auth.mfa_factors` sem nenhuma linha pro usuário de teste — nem
  "unverified". O fator tinha sido removido, não é que nunca existiu.

Causa: a conta salvou usuário/site no app **Senhas do iPhone**, mas nunca
configurou o **Código de Verificação** (TOTP) nele — o campo "Senha"
inclusive ficou vazio. Todo código digitado não vinha de segredo nenhum
válido, daí "incorreto ou expirado" sempre. Separado disso, achamos que o
jeito mais provável de o fator ter sumido do banco foi um clique em
"Remover" durante teste, sem confirmação nenhuma na tela.

- [x] Confirmação antes de remover o autenticador (`GestaoMfa.tsx`), com
      aviso mais forte quando é o último fator ativo da conta.
- [x] Passo 1 do cadastro agora cita o Senhas do iPhone/Android como
      alternativa ao app autenticador dedicado, com o caminho exato
      (Ajustes → Senhas → conta → "Configurar Código de Verificação").
- [x] Confirmado: TOTP é RFC 6238 puro — segredo gerado pelo GoTrue no
      enroll, QR só carrega esse segredo (não tem geração de código no
      servidor por trás do QR). `Cancelar`/reenroll já limpa fator não
      verificado sem deixar resto (`unenroll` antes de gerar QR novo).
- [x] Confirmado por pedido do usuário: MFA **continua opcional**
      (`mfa_obrigatorio = false`) — nenhuma mudança de código necessária,
      já era o comportamento.
- Avaliado e descartado: notificar o código por push do PWA. Quebraria a
  garantia central do TOTP (segredo nunca sai do dispositivo/servidor após
  o enroll) e o código apareceria na tela de bloqueio — mesma fraqueza
  conhecida do SMS OTP. Alternativa melhor pra reduzir fricção, se algum
  dia isso voltar à mesa: WebAuthn/Passkey (Supabase já suporta como tipo
  de fator).

## Convite de usuário novo — corrigido

Segundo incidente do dia: usuário criado em `/admin`, e-mail de convite
chegava, mas o link abria a tela de login normal em vez da tela de definir
senha — sem erro visível nenhum.

Causa: `criarUsuario()` e `enviarLinkRedefinicao()` (`app/actions/usuarios.ts`)
são chamadas de **admin**, do servidor — não tem navegador nenhum
iniciando o fluxo. `/auth/callback` só sabia processar o formato PKCE
(`?code=...`), que depende de um `code_verifier` que só existe quando o
próprio navegador disparou o pedido (é o caso de "Esqueci a senha", que por
isso sempre funcionou). O e-mail de convite do Supabase manda
`token_hash`, nunca `code` — a rota não achava nada, caía num redirect de
erro pra `/login`, e a tela de login engolia o `?erro=` em silêncio.

- [x] `/auth/callback` passa a aceitar `token_hash` + `type` (`verifyOtp`),
      mantendo `code` (`exchangeCodeForSession`) pro fluxo que o navegador
      inicia.
- [x] `/login` mostra aviso quando o link é inválido/expirado, em vez de
      abrir a tela normal sem explicar nada — mesmo padrão de silêncio do
      incidente de MFA, resolvido do mesmo jeito.
- [ ] **Falta o lado do painel** — ver pendência abaixo. Sem ajustar o
      template de e-mail, o código sozinho não resolve.

## Segunda rodada de gaps — o que foi atacado hoje

- [x] **Auditoria de `configuracoes`** — migration `0014`: trigger
      `registrar_auditoria()` também em `configuracoes`. Ligar/desligar
      `mfa_obrigatorio` agora fica em `auditoria`, não só no último
      `atualizado_por`.
- [x] **Erros deixaram de ser engolidos em silêncio** — `perfilAtual()`
      (`lib/dados.ts`) e `estadoMfa()` (`lib/mfa.ts`) logam com
      `console.error` antes de cair no fallback seguro. Foi exatamente o
      tipo de lacuna que atrasa diagnosticar um incidente como o de hoje.
- [x] **Logout automático por inatividade** — `InatividadeLogout.tsx`,
      15 minutos sem interação (mouse, teclado, toque, scroll) e desloga
      sozinho, com aviso na tela de login. Relógio compartilhado entre
      abas via `localStorage`, não é só a memória do componente.
- [x] **Senha mínima 8 + letras e números** reforçada em `/auth/redefinir`
      (única tela que define senha — convite e "esqueci a senha" caem
      nela). **Continua faltando o lado do Supabase**: hoje é validação só
      de navegador, o GoTrue do projeto aceita 6 caracteres sem exigir
      letra+número. Ver pendência abaixo — é config de painel, não código.
- [x] **`setores.efetivo` ligado de ponta a ponta** — como usar:
      1. **Configurações → aba Setores** — cada setor agora tem um campo
         "Efetivo" ao lado do nome. Preenche quantas pessoas trabalham
         naquele setor hoje (aproximado serve, dá pra corrigir depois a
         qualquer momento, sem precisar de SQL). Setor sem número ali
         continua funcionando normal em todo o resto do sistema — só não
         entra na conta nova.
      2. **Indicadores** — card novo "Incidência por 100 colaboradores".
         Antes a tela só mostrava contagem bruta ("Evisceração: 15 casos"),
         que não diz se o setor está pior ou só tem mais gente. Com o
         efetivo preenchido, o card mostra a taxa (casos ÷ efetivo × 100) —
         aí dá pra comparar Evisceração (400 pessoas) com Cone (50
         pessoas) de forma justa. Setor sem efetivo aparece na lista do
         mesmo jeito, só marcado "sem efetivo" em vez de inventar um
         número errado.
      3. Preencher é opcional e não precisa ser exato — o card só aparece
         calculado pros setores que tiverem o número; os outros continuam
         mostrando quantidade de casos normalmente.
- [x] **Formulário de lançamento — conferido, já gravava de verdade.**
      `PENDENCIAS.md` (13/09) estava desatualizado nesse ponto:
      `salvarRemanejamento()` já faz o insert completo, com a lógica de
      reaproveitar colaborador sem matrícula que entrou na rodada de
      16/09. Nenhuma mudança necessária — só a documentação estava velha.
- [x] **Testes automatizados de `situacaoDe()`** — Vitest configurado
      (`vitest.config.ts`, `npm test`), 14 casos em `lib/calculos.test.ts`
      cobrindo os 6 ramos da função e a fronteira exata da data de
      previsão, cada um comentado com o `when` equivalente do
      `situacao_remanejamento()` no Postgres. Cobre também
      `incidenciaPorSetor()` (função nova de hoje). Não roda o lado
      Postgres (sem banco neste ambiente) — a paridade continua manual,
      mas agora documentada teste a teste.
- [x] **Backup sem trocar de plano** — `.github/workflows/backup-banco.yml`:
      `pg_dump` diário, criptografado (GPG simétrico), guardado numa
      branch órfã `backups` deste repositório, com poda automática (mantém
      30 dumps). Precisa de dois secrets no GitHub (`SUPABASE_DB_URL`,
      `BACKUP_GPG_PASSPHRASE`) — instruções no topo do arquivo do
      workflow. Ninguém além de quem tem acesso ao Settings do repositório
      chega nesses segredos. **Ainda não está rodando de fato** — ver
      pendência abaixo, precisa dos 2 secrets configurados primeiro.

## Terceira rodada — headers, observabilidade, auditoria, um bug real

- [x] **Headers de segurança** — `middleware.ts` passa a mandar CSP (com
      nonce por requisição no `script-src`, então o Next continua
      funcionando sem precisar de `unsafe-inline` nos próprios scripts),
      `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`,
      `Referrer-Policy`, `Permissions-Policy`. `next.config.js` ganhou
      `poweredByHeader: false`. Validado de verdade: `npm run build` +
      `next start` + `curl` nos headers de resposta, não só lido no código.
- [x] **Observabilidade interna** (migration `0015`) — tabela
      `erros_aplicacao`, com insert liberado mesmo sem sessão (é onde os
      dois incidentes de hoje teriam aparecido, antes do login) e leitura
      só pra admin. `lib/registrarErro.ts` grava nela a partir dos catches
      de `perfilAtual()`/`estadoMfa()`. `app/error.tsx` (novo) pega erro de
      renderização de client component — o único jeito de capturar isso,
      nunca passa pelo servidor. Aba **Erros** nova em `/admin`, só admin,
      mostra os últimos 50. Não substitui Sentry — não agrega, não alerta,
      não tem retenção configurável. É o suficiente pra não depender de
      alguém reclamar.
- [x] **Auditoria da service role key** — só 2 chamadas no projeto inteiro
      (`listarUsuarios()` e `criarUsuario()`, as duas em
      `app/actions/usuarios.ts`), as duas legítimas: pedem algo que só a
      Admin API do GoTrue alcança (`auth.users`, `inviteUserByEmail`,
      `deleteUser`). Nada pra remover — documentado em
      `lib/supabase-admin.ts` pra quem for adicionar um uso novo parar e
      perguntar se dá pra fazer sem.
- [x] **Bug real achado testando** — `estatisticaDuracao()` calculava a
      mediana errada sempre que o número de casos era par: pegava só o
      valor de cima (`dias[Math.floor(n/2)]`) em vez da média dos dois do
      meio. Ex.: durações `[10, 20, 30, 40]` devolvia mediana 30; o certo é
      25. Corrigido, com teste cobrindo par e ímpar — 17 testes no total
      agora.

## Pendências que ficaram — e por quê

- [ ] **Ajustar o template "Invite user" no Supabase** (Authentication →
      Emails → Invite user) pra apontar com `token_hash` — sem isso, o fix
      de código do convite não entra em vigor. **Bloqueado por um degrau
      antes**: o painel não deixa editar Subject/Body do template
      enquanto o projeto usa o serviço de e-mail padrão do Supabase —
      precisa configurar **SMTP próprio** primeiro (botão "Set up SMTP" na
      mesma tela). Decisão de qual provedor de SMTP usar fica com o
      usuário — Resend, Gmail/Workspace da GTF ou outro que já tenham.
      Serve pra dois pássaros: o mesmo SMTP resolve o convite **e** vira
      candidato natural pro provedor de e-mail da notificação de
      vencimento (item mais abaixo).
- [ ] **Senha mínima 8 + letras/números no Supabase.** É config de painel
      (Authentication → Policies → Password Requirements), não SQL nem
      código — precisa ser feito no dashboard.
- [ ] **Desligar "Allow new users to sign up"** — mesma natureza, painel.
- [ ] **Ativar o backup** — o workflow existe (`.github/workflows/backup-banco.yml`)
      mas não roda até os secrets `SUPABASE_DB_URL` e
      `BACKUP_GPG_PASSPHRASE` serem cadastrados em Settings → Secrets and
      variables → Actions do repositório.
- [ ] **Notificação de vencimento de prazo.** Não atacado, por decisão do
      usuário: depende de escolher provedor (e-mail — Resend/SMTP da GTF —
      ou push do PWA via VAPID) antes de construir.
- [ ] **Duplicidade Geovani/Giovanni e Valdeir/Valdeci** — usuário pediu
      pra ver depois.
- [ ] **Inversão clínico×ocupacional 2025→2026** — segue sem confirmação
      interna antes de virar indicador oficial.
- [ ] **Contrato com a GTF** (propriedade de código, responsabilidade
      sobre dado, SLA) — explicitamente adiado pelo usuário. Fica
      registrado aqui como pendência formal, não esquecido.
- [ ] **Proposta comercial** (implantação R$ 6.000; mensalidade R$ 750 /
      1.200 / 2.000 por porte; Supabase Pro ~R$ 140/mês à parte) — mesma
      situação do contrato, sem ligação direta com código.
- [ ] **Recovery code de MFA** — avaliado, adiado por decisão do usuário
      ("ok fazemos depois").

## Gaps novos, achados hoje

- Next 14.2.15 tem CVE conhecida (`npm install` avisa na cara) — vale
  planejar upgrade. Não atacado por decisão do usuário ("não precisa
  fazer o 1" referia-se a rate limit, mas upgrade de versão também ficou
  de fora do escopo de hoje).
- Sem rate limit / lockout em tentativas de senha e de código MFA —
  avaliado, não atacado por decisão do usuário.
