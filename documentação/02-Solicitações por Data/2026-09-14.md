---
tags: [solicitacao, "2026-09-14"]
---

# 2026-09-14 — Edição, admin de listas, multi-unidade, relatórios, usuários

Segundo dia: lote grande de funcionalidades pedidas de uma vez (o usuário
prefere mandar várias mudanças e só depois pedir para implementar tudo
junto).

## Pedido e feito

### Relatórios
- [x] Gerar relatórios que possam ser impressos (cabeçalho só de
      impressão, filtros ocultos ao imprimir)
- [x] Deixar selecionável se quer gerar PDF ou CSV
- [x] Filtro por múltiplos setores, gráficos de BI, drill-down em todos os
      gráficos/indicadores

### Editar casos sem sobrescrever
- [x] Tela de edição de remanejamento (`FormularioEdicao`,
      `/remanejamentos/[id]/editar`) — nome e matrícula ficam travados de
      propósito, para não quebrar o vínculo com o histórico do
      colaborador
- [x] Histórico automático da edição via trigger de auditoria (não
      precisou de código extra: o trigger já existia desde `0001_schema.sql`
      e cobre INSERT/UPDATE/DELETE)

### Arquitetura multi-unidade (implementar pensando no futuro)
- [x] Código e migration escritos: tabela `unidades`, coluna
      `unidade_id` em todas as tabelas, função `minha_unidade_id()`, RLS
      por unidade (`supabase/migrations/0006_multi_unidade.sql`)
- [ ] *(a migration só foi executada de fato no banco em 2026-09-15 — ver
      [[2026-09-15]])*

### Admin de listas
- [x] Tela `/admin` com CRUD de setores, turnos, supervisores,
      profissionais, segmentos e regiões do corpo (criar, renomear,
      ativar/desativar)

### Textos
- [x] "Em carteira" → "Vigentes"
- [x] "Carteira por setor/região" → "Casos por setor/região"

### Gestão de usuários (pedido em duas etapas)
Pedido inicial: alguma forma de o administrador criar usuários já
definindo papel (operador/visualizador) e função, sem usar o painel do
Supabase diretamente — porque só o administrador da plataforma sabe qual
o papel de cada colaborador.

- [x] Tela **Configurações → Usuários**: criar (nome, e-mail, função,
      papel, admin), editar, desativar/reativar, reenviar link de
      redefinição de senha
- [x] Convite por e-mail (Supabase `inviteUserByEmail`) — usuário define a
      própria senha ao clicar no link
- [x] Fluxo PKCE: `/auth/callback` troca o código pela sessão e manda para
      `/auth/redefinir`, onde a pessoa escolhe a senha
- [x] "Esqueci a senha" na tela de login

## Commits do dia

`027920a` — edição de casos, admin de listas, multi-unidade (código),
relatórios
`02f6344` — gestão de usuários no painel admin
